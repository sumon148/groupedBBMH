% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mh_algorithm.R
\name{create_mcmc_BP}
\alias{create_mcmc_BP}
\title{Create MCMC Array for Bayesplot Visualization}
\usage{
create_mcmc_BP(mcmc.list.chain, varnames)
}
\arguments{
\item{mcmc.list.chain}{A list of 4 matrices or data frames, each representing
one MCMC chain. Each matrix should have parameters as columns and iterations as rows.}

\item{varnames}{A character vector of variable names corresponding to the columns
in each chain (e.g., \code{c("log(alpha)", "logit(mu)")}). These will be used
as parameter names in the output array.}
}
\value{
A 3-dimensional array with dimensions (iterations × chains × parameters),
suitable for use with \code{bayesplot} functions such as \code{mcmc_trace()} or
\code{mcmc_dens_overlay()}.
}
\description{
Converts a list of MCMC chains (typically from a Metropolis-Hastings algorithm)
into a 3D array suitable for plotting using the \code{bayesplot} package.
}
\examples{
# Example usage (assuming 4 chains of posterior samples):
\dontrun{
  chains_list <- list(chain1, chain2, chain3, chain4)
  param_names <- c("log(alpha)", "logit(mu)")
  mcmc_array <- create_mcmc_BP(chains_list, param_names)
  bayesplot::mcmc_trace(mcmc_array)
}

}
